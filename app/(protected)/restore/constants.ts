/**
 * Superadmin Restore Page Constants
 * Mirrors the backup page constants — kept in sync.
 */

export { SYNC_THRESHOLD_BYTES, MAX_RESTORE_FILE_BYTES, POLL_INTERVAL_MS, ACCEPTED_EXTENSIONS, FORMATTING, STATUS_LABELS, MODE_LABELS } from '../backup/constants';
